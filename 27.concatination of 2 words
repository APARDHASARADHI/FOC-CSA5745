#include <stdio.h>
#include <string.h>
 
int main()
{
 
    char s[] = "pardhu";
    char s1[] = "chandu";
 
    // concatenating the string
    strcat(s, s1);
    printf("Final string is: %s ", s);
    return 0;
}
